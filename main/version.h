#pragma once

// Bump this only when the app requires a specific SPIFFS asset set.
// Firmware version is generated automatically from ESP-IDF's PROJECT_VER,
// which follows the current Git tag for release builds.
#define NOWTUBE_ASSET_REV "0.6"
